export type Hero = {
    id: string,
    speed: number,
    x: number,
    y: number
}

export type OtherHero = {
    id: string,
    x: number,
    y: number
}

export type Monster = {
    x: number,
    y: number
}